/** Re-export registration schema from location module (single source of truth). */
export {
  registerLocationSchema,
  updateLocationSchema,
  type RegisterLocationInput,
  type UpdateLocationInput,
} from "@/lib/location";
